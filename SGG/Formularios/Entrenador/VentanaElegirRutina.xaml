<Window x:Class="SGG.Formularios.Entrenador.VentanaElegirRutina"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SGG - Asignar Rutina" Height="550" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#0D0D0D">

	<Grid Margin="25">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<DockPanel Grid.Row="0" Margin="0,0,0,4">
			<Button DockPanel.Dock="Right" Content="+ Crear Nueva Rutina"
                    Background="#A855F7" Foreground="White" BorderThickness="0"
                    Padding="12,7" Click="btnCrearNueva_Click"/>
			<TextBlock Text="Asignar Rutina" FontSize="22" FontWeight="Bold" Foreground="White"/>
		</DockPanel>
		<TextBlock Grid.Row="1" Text="Elegí una rutina existente como base, o creá una nueva desde cero"
                    FontSize="12" Foreground="#888888" Margin="0,0,0,20"/>

		<ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
			<ItemsControl x:Name="icRutinasDisponibles">
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1"
                                CornerRadius="6" Padding="14" Margin="0,0,0,10">
							<DockPanel>
								<Button DockPanel.Dock="Right" Content="Asignar"
                                        Click="btnAsignar_Click" Tag="{Binding}"
                                        Background="#2A2A3D" Foreground="#A855F7" BorderBrush="#A855F7"
                                        Padding="10,6" VerticalAlignment="Center"/>
								<StackPanel VerticalAlignment="Center">
									<TextBlock Text="{Binding Nombre}" Foreground="White" FontWeight="Bold" FontSize="13"/>
									<TextBlock Text="{Binding Nivel}" Foreground="#888888" FontSize="11" Margin="0,2,0,0"/>
								</StackPanel>
							</DockPanel>
						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
	</Grid>
</Window>
